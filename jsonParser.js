const fs = require('fs')

// make a crazy super object, like a death star, that 
// holds all of our data (it's constant because we don't
// allow changes from the outside)

const tableNames = [
  'films', 'people', 'planets', 'species', 'starships', 'transport', 'vehicles'
]